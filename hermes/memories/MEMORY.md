Google Search Console CarCrew — verificado via DNS TXT record
§
WhatsApp CarCrew: (44) 99813-3182 (confirmado). Presente em Footer, Header, ProductCard e JSON-LD.
§
erc20TokenLab (github.com/linikers/erc20TokenLab): curso Web3 R$19 em ecr20ttk.vercel.app. Pix estático funcionando (QR code aparece). PagBank sem permissão de venda (app ttktoken). X para vender: @hermesBigAgent via xurl CLI (sem crédito na API).
§
Prefere posts de X/Twitter com tom leve, curioso e construtivo. NUNCA revoltado, negativo ou reclamão. "vc esta muito revoltado, paz no coração" — corrigiu explicitamente tom de desabafo. Posts devem soar como observação interessante, não reclamação.
§
CarCrewCommerce (carcrew.com.br): "Car Crew Garage". GBP share link nos sameAs. Sitemap 51 URLs. 46 produtos ativos (R$0). Melhor Envio configurado (PR #36). GSC verificado DNS.
§
autohedge-bot (linikers/autohedge-bot): publicado no GitHub. Pipeline Director→Quant→Risk→Execution. CLI apenas, sem interface web. Precisa OPENCODE_API_KEY pra rodar. Pendente: definir frequencia de execucao e configurar wallet.
§
Portfolio (linikers/portfolio, deploy linikers-portfolio.vercel.app): Pages Router, MUI v6, framer-motion v11. Passou por cleanup (issue #30). Navbar com PERFIL, BoxDev padronizado com cards, CTA "$ iniciar conversa" na home.
§
zap-hub (linikers/zap-hub): repo de conexoes WhatsApp + backup do Hermes. drivers/ (cloud-api=simples, baileys=QR complexo), bots/ (nfe-baileys, ml-atendente-cloud), hermes/ (backup diario 8h com skills, memorias, crons, facts). Cron backup: 4df8558181f2.
§
Taiff Connect (github.com/SistemasTaiffProart/taiff-connect-backend): Node + Express 5 + TypeORM + PostgreSQL. Twilio Verify OTP implementado e na main (PR#23 mergeado, build passando). Erro comum: import dinamico do `twilio` SDK v5 precisa de `.default()` (TwilioProvider.ts e TwilioVerifyProvider.ts). Dependencia adicionada no yarn.lock (nao esquecer de rodar yarn install ao adicionar novas deps).